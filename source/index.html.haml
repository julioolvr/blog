---
pageable: true
---

- page_articles.each_with_index do |article, i|
  %article.index-post
    %h2= link_to article.title, article
    .index-post-date= article.date.strftime('%b %e')
    ~ article.summary
- if paginate
  - if prev_page
    %p= link_to 'Anterior', prev_page
  - if next_page
    %p= link_to 'Siguiente', next_page
